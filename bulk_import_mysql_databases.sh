#for my use at least you can't have a space between -p and the DB password.
#repeat these variables as much or as little as needed.

mysql -u DB_USERNAME -pDB_PASSWORD DB_NAME < DB_EXPORT.sql;
mysql -u DB_USERNAME -pDB_PASSWORD DB_NAME < DB_EXPORT.sql;
mysql -u DB_USERNAME -pDB_PASSWORD DB_NAME < DB_EXPORT.sql;
